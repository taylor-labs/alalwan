<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title data-title-en="{% if title %}{{ title }} | Shaykh Sulayman al-Alwan{% else %}Shaykh Sulayman al-Alwan{% endif %}" data-title-ar="{% if title_ar %}{{ title_ar }} | الشيخ سليمان بن ناصر العلوان{% endif %}">{% if title %}{{ title }} | Shaykh Sulayman al-Alwan{% else %}Shaykh Sulayman al-Alwan{% endif %}</title>
  <meta name="description" content="{{ description or 'A curated archive of scholarly works, fatawa, and translations from Shaykh Sulayman al-Alwan.' }}">
  <script>
    (function() {
      var saved = localStorage.getItem('alwan_theme');
      var prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
      if (saved === 'dark' || (!saved && prefersDark)) {
        document.documentElement.setAttribute('data-theme', 'dark');
      }
    })();
    (function() {
      if (localStorage.getItem('alwan_lang') === 'ar') {
        document.documentElement.setAttribute('data-lang', 'ar');
        document.documentElement.setAttribute('dir', 'rtl');
        document.documentElement.setAttribute('lang', 'ar');
      }
    })();
  </script>
  <link rel="icon" href="/static/img/favicon.ico" sizes="any">
  <link rel="icon" type="image/png" sizes="32x32" href="/static/img/favicon-32x32.png" media="(prefers-color-scheme: light)">
  <link rel="icon" type="image/png" sizes="16x16" href="/static/img/favicon-16x16.png" media="(prefers-color-scheme: light)">
  <link rel="icon" type="image/png" sizes="32x32" href="/static/img/favicon-32x32-dark.png" media="(prefers-color-scheme: dark)">
  <link rel="icon" type="image/png" sizes="16x16" href="/static/img/favicon-16x16-dark.png" media="(prefers-color-scheme: dark)">
  <link rel="apple-touch-icon" sizes="180x180" href="/static/img/apple-touch-icon.png">
  <link rel="icon" type="image/png" sizes="192x192" href="/static/img/favicon-192x192.png">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=EB+Garamond:ital,wght@0,400;0,500;1,400&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Scheherazade+New:wght@400;700&family=Cairo:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/static/css/style.css">
</head>
<body>
  <div id="progress-bar"></div>
  <header>
    <div class="header-left">
      <a href="/"><img src="/static/img/al-alwan.png" alt="سليمان ناصر العلوان" class="site-banner"></a>
    </div>
    <nav id="main-nav">
      <a href="/"><span data-lang-en>Home</span><span data-lang-ar lang="ar" dir="rtl" class="ar-label">الرئيسية</span></a>
      <a href="/biography/"><span data-lang-en>Biography</span><span data-lang-ar lang="ar" dir="rtl" class="ar-label">السيرة الذاتية</span></a>
      <a href="/fatawa/"><span data-lang-en>Fatawa</span><span data-lang-ar lang="ar" dir="rtl" class="ar-label">الفتاوى</span></a>
      <a href="/sciences/"><span data-lang-en>Sciences</span><span data-lang-ar lang="ar" dir="rtl" class="ar-label">العلوم</span></a>
      <a href="/about/"><span data-lang-en>About</span><span data-lang-ar lang="ar" dir="rtl" class="ar-label">حول</span></a>
    </nav>
    <div class="header-right">
      <button class="lang-toggle" id="lang-toggle" aria-label="Switch language">AR</button>
      <button class="theme-toggle" id="theme-toggle" aria-label="Toggle dark mode"></button>
      <button class="menu-toggle" id="menu-toggle" aria-label="Open menu" aria-expanded="false">
        <span class="menu-toggle-bar"></span>
        <span class="menu-toggle-bar"></span>
        <span class="menu-toggle-bar"></span>
      </button>
    </div>
  </header>
  <div class="mobile-menu" id="mobile-menu" aria-hidden="true">
    <a href="/"><span data-lang-en>Home</span><span data-lang-ar lang="ar" dir="rtl" class="ar-label">الرئيسية</span></a>
    <a href="/biography/"><span data-lang-en>Biography</span><span data-lang-ar lang="ar" dir="rtl" class="ar-label">السيرة الذاتية</span></a>
    <a href="/fatawa/"><span data-lang-en>Fatawa</span><span data-lang-ar lang="ar" dir="rtl" class="ar-label">الفتاوى</span></a>
    <a href="/sciences/"><span data-lang-en>Sciences</span><span data-lang-ar lang="ar" dir="rtl" class="ar-label">العلوم</span></a>
    <a href="/about/"><span data-lang-en>About</span><span data-lang-ar lang="ar" dir="rtl" class="ar-label">حول</span></a>
  </div>

  <main>
    {{ content | safe }}
  </main>

  <footer>
    <div class="footer-inner">
      <span class="footer-name">Shaykh Sulayman al-Alwan</span>
      <span class="footer-sep">·</span>
      <span data-lang-en>Fatawa, lectures, and writings — in Arabic and English</span>
      <span data-lang-ar lang="ar" dir="rtl" class="ar-label">الفتاوى والمحاضرات والكتابات — بالعربية والإنجليزية</span>
    </div>
  </footer>

  <script>
    // Active nav highlight
    document.querySelectorAll('nav a').forEach(function(link) {
      var path = window.location.pathname;
      if (link.getAttribute('href') === '/' ? path === '/' : path.startsWith(link.getAttribute('href'))) {
        link.classList.add('active');
      }
    });

    // Scroll progress bar
    var bar = document.getElementById('progress-bar');
    function updateProgress() {
      var total = document.documentElement.scrollHeight - window.innerHeight;
      bar.style.width = total > 0 ? (window.scrollY / total * 100) + '%' : '0%';
    }
    window.addEventListener('scroll', updateProgress, { passive: true });
    updateProgress();

    // Theme toggle
    var moonSVG = '<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M13.5 9.5A6 6 0 0 1 6.5 2.5a6 6 0 1 0 7 7z"/></svg>';
    var sunSVG  = '<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="8" cy="8" r="3"/><path d="M8 1v2M8 13v2M1 8h2M13 8h2M3.22 3.22l1.42 1.42M11.36 11.36l1.42 1.42M3.22 12.78l1.42-1.42M11.36 4.64l1.42-1.42"/></svg>';

    var toggleBtn = document.getElementById('theme-toggle');

    function applyThemeIcon() {
      var dark = document.documentElement.getAttribute('data-theme') === 'dark';
      toggleBtn.innerHTML = dark ? sunSVG : moonSVG;
    }

    toggleBtn.addEventListener('click', function() {
      var dark = document.documentElement.getAttribute('data-theme') === 'dark';
      var next = dark ? 'light' : 'dark';
      document.documentElement.setAttribute('data-theme', next);
      localStorage.setItem('alwan_theme', next);
      applyThemeIcon();
    });

    applyThemeIcon();

    // Language toggle
    var langToggleBtn = document.getElementById('lang-toggle');

    function applyLangLabel() {
      var isAr = document.documentElement.getAttribute('data-lang') === 'ar';
      langToggleBtn.textContent = isAr ? 'EN' : 'AR';
    }

    function updatePlaceholders() {
      var isAr = document.documentElement.getAttribute('data-lang') === 'ar';
      document.querySelectorAll('[data-placeholder-en]').forEach(function(el) {
        el.setAttribute('placeholder', isAr ? el.getAttribute('data-placeholder-ar') : el.getAttribute('data-placeholder-en'));
      });
    }

    function updateTitle() {
      var isAr = document.documentElement.getAttribute('data-lang') === 'ar';
      var titleEl = document.querySelector('title');
      var titleAr = titleEl.getAttribute('data-title-ar');
      var titleEn = titleEl.getAttribute('data-title-en');
      document.title = (isAr && titleAr) ? titleAr : titleEn;
    }

    langToggleBtn.addEventListener('click', function() {
      var isAr = document.documentElement.getAttribute('data-lang') === 'ar';
      var next = isAr ? 'en' : 'ar';
      if (next === 'ar') {
        document.documentElement.setAttribute('data-lang', 'ar');
        document.documentElement.setAttribute('dir', 'rtl');
        document.documentElement.setAttribute('lang', 'ar');
      } else {
        document.documentElement.removeAttribute('data-lang');
        document.documentElement.setAttribute('dir', 'ltr');
        document.documentElement.setAttribute('lang', 'en');
      }
      localStorage.setItem('alwan_lang', next);
      applyLangLabel();
      updatePlaceholders();
      updateTitle();
    });

    applyLangLabel();
    updatePlaceholders();
    updateTitle();

    // Mobile menu toggle
    var menuBtn = document.getElementById('menu-toggle');
    var mobileMenu = document.getElementById('mobile-menu');
    menuBtn.addEventListener('click', function() {
      var open = mobileMenu.classList.toggle('open');
      menuBtn.classList.toggle('open', open);
      menuBtn.setAttribute('aria-expanded', open);
      mobileMenu.setAttribute('aria-hidden', !open);
    });
    document.addEventListener('click', function(e) {
      if (!mobileMenu.contains(e.target) && !menuBtn.contains(e.target)) {
        mobileMenu.classList.remove('open');
        menuBtn.classList.remove('open');
        menuBtn.setAttribute('aria-expanded', 'false');
        mobileMenu.setAttribute('aria-hidden', 'true');
      }
    });

    // Active nav highlight (mobile menu too)
    document.querySelectorAll('.mobile-menu a').forEach(function(link) {
      var path = window.location.pathname;
      if (link.getAttribute('href') === '/' ? path === '/' : path.startsWith(link.getAttribute('href'))) {
        link.classList.add('active');
      }
    });
  </script>
</body>
</html>
